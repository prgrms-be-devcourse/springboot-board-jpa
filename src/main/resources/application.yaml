spring:
  datasource:
    url: jdbc:mysql://localhost:3310/board
    username: root
    password: root1234!
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        ddl-auto: create
