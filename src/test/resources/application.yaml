spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
  output:
    ansi:
      enabled: always

cookie:
  max-age: 600

logging:
  level:
    org.hibernate.SQL: debug

decorator:
  datasource:
    p6spy:
      enable-logging: true